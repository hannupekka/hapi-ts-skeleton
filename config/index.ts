const {
  API_VERSION,
  DB_NAME,
  DB_PASS,
  DB_URL,
  DB_USER,
  NODE_ENV,
  PORT,
  REQUIRE_HTTPS,
} = process.env;

const config = {
  API_VERSION,
  DB_NAME,
  DB_PASS,
  DB_URL,
  DB_USER,
  NODE_ENV,
  PORT,
  REQUIRE_HTTPS,
};

export default config;
